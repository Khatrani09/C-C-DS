#include <stdio.h>
void main()
{

    // int i = 36;
    // i = !i > 37;
    // printf("i = %d", i);
    // 1 ) ANS :
    // float c[10];
    // printf("%d\n ", abs(&c[3] - &c[7]) / (&c[7] - &c[3]));

    // 2 ) ANS :
    // int a = 5, b = 0, m, n;
    // m = a++ && b++;
    // n = --a || --b;
    // printf("%d %d %d %d", a, b, m, n);

    // 3) ANS :
    // do
    // {
    //     printf(" be ");
    // } while (!printf(" you "));

    // 4 ) ANS :
    //     int i = 5;
    // N:
    //     printf("%d ", i);
    //     i += 5;
    //     if (i <= 15)
    //         goto N;

    // 5 ) ANS :
    // int x = 30, y = 30;
    // if (!(x && y))
    //     printf(" never settle ");
    // else
    //     printf(" be you ");

    // 6 ) ANS :
    // int i = 30, j = 300, n = 3000;
    // n += j % i++ + j % i--;
    // printf("%d\n", n);

    // 7 ) ANS :
    // int x = 0, z;
    // z = (x || 1) ? (x || 1) : (x && 1);
    // printf("%d", z);

    // 8 ) ANS :
    // char str[50] = "dont copy";
    // int n = printf("\n%s", str);
    // printf("\nn : %d", n);

    // 9 ) ANS :
    // int i = 3;
    // while (8 - i++)
    //     printf("%d", i);

    // 10 ) ANS :
    // int i = 30, j = 40, k = 50;
    // i = j, j = k, k = i;
    // printf(" ", printf("%d", i++), printf("%d", --j), printf("%d", k++));

    // 11 ) ANS :
    // char character = 'S';
    // int number = 32, value;
    // value = character + number;
    // printf("%c -  %d", value, value);

    // 12 ) ANS :
    // int i = 5, j = 5;
    // for (--i &&j++; i < 15; i += 2)
    //     printf("loop ");

    // 13 ) ANS :
    // int a, b;
    // printf("%d", (scanf("%d %d", &a, &b)));

    // 14 )ANS :
}

// 0 1 2 1 0 1 keep going 2 4 6 8 be you 1001 1 12 1 2 3 4 5 20 29 20 a - 97 loop * 5 2
